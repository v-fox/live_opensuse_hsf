##################
# video settings #
##################

# Specify default video driver (see -vo help for a list).
vo=opengl,xv,x11,

# Start in fullscreen mode by default.
fs=no

##################
# audio settings #
##################

# Specify default audio driver (see -ao help for a list).
ao=jack:std-channel-layout=waveext,pulse,alsa,oss,esd,arts,

# Output audio to S/PDIF
#ao=alsa:device=spdif
#ac=hwac3,hwdts,hwmpa,

# default volume
#softvol=yes
#volume=15
#af=volume=1
af=volnorm=1,scaletempo

##################
# other settings #
##################

# Drop frames to preserve audio/video sync.
#framedrop = yes

# cache settings
#
# Use 8MB input cache by default.
#cache = 8192

# get a default OSD font from fontconfig
fontconfig=yes
font="DejaVu Sans"
subfont-text-scale=3
subfont-osd-scale=4

# disable joystick by default as it interferes with notebook motion sensors
#nojoystick=yes

# disable the running screen saver
stop-xscreensaver=1
heartbeat-cmd="xscreensaver-command -deactivate >&- 2>&- &"
